const React = require("react");
const Nav = require("Nav");

var Main = (props) => {
        return (
            <div>
                <Nav />
            </div>
        )
    };


module.exports = Main;